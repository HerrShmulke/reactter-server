type Query {
  posts: [Post!]
  post(id: ID!): Post
}

type Mutation {
  createPost(postCreateInput: PostCreateInput): Boolean
}

type Post {
  id: ID!
  message: String!
}

input PostCreateInput {
  message: String!
}
