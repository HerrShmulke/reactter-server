type Query {
  user(id: ID!): User
}

type Mutation {
  registerUser(userRegisterInput: UserRegisterInput): Boolean
}

type User {
  id: ID!
  name: String!
  avatarUrl: String
}

input UserRegisterInput {
  name: String!
  password: String!
}
